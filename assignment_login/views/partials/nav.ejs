<nav class="navbar navbar-expand-lg navbar-dark py-lg-4" id="mainNav">
    <div class="container">
        <a class="navbar-brand text-uppercase text-expanded font-weight-bold d-lg-none" href="#">Start Bootstrap</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
            aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul id="navlist" class="navbar-nav mx-auto">
                <% if (pagename=="home") {%>
                <li class="nav-item active px-lg-4">
                    <% } else { %>
                <li class="nav-item px-lg-4">
                    <% } %>
                    <a class="nav-link text-uppercase text-expanded" href="/">Home</a>
                </li>
                <% if (pagename=="about") { %>
                <li class="nav-item active px-lg-4">
                    <% } else { %>
                <li class="nav-item px-lg-4">
                    <% } %>
                    <a class="nav-link text-uppercase text-expanded" href="/about">About</a>
                </li>

                <% if (typeof(sess) != 'undefined' && sess.loggedin==true) {%>
                <% if (pagename=="profile") { %>
                <li class="nav-item active px-lg-4">
                    <% } else { %>
                <li class="nav-item px-lg-4">
                    <% } %>
                    <a class="nav-link text-uppercase text-expanded" href="/profile">Profile</a>
                </li>
                <li class="nav-item px-lg-4">
                    <a class="nav-link text-uppercase text-expanded" href="/logout">Logout</a>
                </li>
                <%}%>
            </ul>
        </div>
    </div>
</nav>

<div
    style="z-index: 12; display: block; text-align: right; background-color: #333a40; margin-right: 0px; padding-right: 20em; color: gray; position: absolute; width: 155px; margin-top: 4px; padding-top: 5px; right: 0; border-radius: 10px; padding: 11px; font-size: 12px;">
    <form action="/login" method="POST">
        <input type="email" name="email" placeholder="Email Address">
        <input type="password" name="password" placeholder="Password">
        <button type="submit">Log In</button>
    </form>
</div>

<div class="d-flex flex-column w-100">
    <% if (typeof(errors)!="undefined") { %>
                <% for(x in errors) {%>
                <br>
                <span class="text-center text-white mt-4"><%= errors[x] %></span>
                <%}%>
    <%}%>
        </div>
        <div class="d-flex justify-content-center w-100">
            <% if (typeof(success)!="undefined") { %>
            <span class="text-center text-white mt-4"><%= success %></span>
            <%}%>
    </div>